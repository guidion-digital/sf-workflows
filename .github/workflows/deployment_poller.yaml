name: Poll Salesforce Deployment Status

# Reusable poller workflow. Consuming repos should call this on a schedule:
#
#   name: Poll Salesforce Deployments
#   on:
#     schedule:
#       - cron: '*/10 6-19 * * 1-5'
#     workflow_dispatch:
#   jobs:
#     poll:
#       permissions:
#         actions: write
#         contents: read
#       uses: guidion-digital/sf-workflows/.github/workflows/deployment_poller.yaml@v1
#       secrets: inherit

on:
  workflow_call:
  # Direct schedule — uncomment if running the poller standalone in this repo
  # Cron runs in UTC — covers 8:00–20:00 CET (7:00–19:00 UTC) / CEST (6:00–18:00 UTC)
  # schedule:
  #   - cron: '*/10 6-19 * * 1-5'
  workflow_dispatch:

permissions:
  actions: write
  contents: read

concurrency:
  group: deployment-poller-${{ github.repository }}
  cancel-in-progress: false

jobs:
  find-artifacts:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.find.outputs.matrix }}
      has_pending: ${{ steps.find.outputs.has_pending }}
    steps:
      - name: Find pending deployment artifacts
        id: find
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Searching for pending deployment artifacts..."

          ARTIFACTS=$(gh api "repos/${{ github.repository }}/actions/artifacts?per_page=100" \
            --jq '[.artifacts[] | select(.name | startswith("pending-deployment-")) | {artifact_id: (.id | tostring), artifact_name: .name}]')

          COUNT=$(echo "$ARTIFACTS" | jq 'length')

          if [ "$COUNT" -eq 0 ]; then
            echo "No pending deployments found."
            echo "has_pending=false" >> "$GITHUB_OUTPUT"
            echo 'matrix={"include":[]}' >> "$GITHUB_OUTPUT"
          else
            echo "Found $COUNT pending deployment(s):"
            echo "$ARTIFACTS" | jq '.'
            echo "has_pending=true" >> "$GITHUB_OUTPUT"
            echo "matrix=$(echo "$ARTIFACTS" | jq -c '{include: .}')" >> "$GITHUB_OUTPUT"
          fi

  process-deployment:
    needs: find-artifacts
    if: needs.find-artifacts.outputs.has_pending == 'true'
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJSON(needs.find-artifacts.outputs.matrix) }}
      fail-fast: false
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      SLACK_TOKEN: ${{ secrets.SLACK_TOKEN }}
      SF_LOGIN_URL_DEV: ${{ vars.SF_LOGIN_URL_DEV }}
      SF_USERNAME_DEV: ${{ vars.SF_USERNAME_DEV }}
      SF_CLIENT_ID_DEV: ${{ vars.SF_CLIENT_ID_DEV }}
      SF_CLIENT_KEY_DEV: ${{ secrets.SF_CLIENT_KEY_DEV }}
      SF_LOGIN_URL_ACC: ${{ vars.SF_LOGIN_URL_ACC }}
      SF_USERNAME_ACC: ${{ vars.SF_USERNAME_ACC }}
      SF_CLIENT_ID_ACC: ${{ vars.SF_CLIENT_ID_ACC }}
      SF_CLIENT_KEY_ACC: ${{ secrets.SF_CLIENT_KEY_ACC }}
      SF_LOGIN_URL_PROD: ${{ vars.SF_LOGIN_URL_PROD }}
      SF_USERNAME_PROD: ${{ vars.SF_USERNAME_PROD }}
      SF_CLIENT_ID_PROD: ${{ vars.SF_CLIENT_ID_PROD }}
      SF_CLIENT_KEY_PROD: ${{ secrets.SF_CLIENT_KEY_PROD }}
      SF_BASE_URL_DEV: ${{ vars.SF_BASE_URL_DEV }}
      SF_BASE_URL_ACC: ${{ vars.SF_BASE_URL_ACC }}
      SF_BASE_URL_PROD: ${{ vars.SF_BASE_URL_PROD }}
      CACHE_FLUSH_API_KEY: ${{ secrets.CACHE_FLUSH_API_KEY }}
      USER_ID_FOR_CACHE_FLUSH: ${{ vars.USER_ID_FOR_CACHE_FLUSH }}
    steps:
      - name: Cache Salesforce CLI
        id: cache-sf
        uses: actions/cache@v4
        with:
          path: /usr/local/sf-cli
          key: sf-cli-stable-linux-x64

      - name: Install Salesforce CLI
        if: steps.cache-sf.outputs.cache-hit != 'true'
        run: |
          wget -q https://developer.salesforce.com/media/salesforce-cli/sf/channels/stable/sf-linux-x64.tar.xz
          mkdir -p /usr/local/sf-cli
          tar xJf sf-linux-x64.tar.xz -C /usr/local/sf-cli --strip-components=1
          rm sf-linux-x64.tar.xz

      - name: Add Salesforce CLI to PATH
        run: echo "/usr/local/sf-cli/bin" >> "$GITHUB_PATH"

      - name: Download artifact
        id: download
        run: |
          mkdir -p artifact_temp
          if ! gh api "repos/${{ github.repository }}/actions/artifacts/${{ matrix.artifact_id }}/zip" > artifact.zip 2>/dev/null; then
            echo "::warning::Failed to download artifact ${{ matrix.artifact_name }} (likely expired)"
            echo "success=false" >> "$GITHUB_OUTPUT"
            echo "DELETE_ARTIFACT=true" >> "$GITHUB_ENV"
          else
            unzip -o artifact.zip -d artifact_temp
            echo "success=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Parse and validate deployment info
        if: steps.download.outputs.success == 'true'
        id: parse
        run: |
          if [ ! -f artifact_temp/deployment-info.json ]; then
            echo "::error::deployment-info.json not found in artifact ${{ matrix.artifact_name }}"
            echo "valid=false" >> "$GITHUB_OUTPUT"
            echo "DELETE_ARTIFACT=true" >> "$GITHUB_ENV"
            exit 0
          fi

          INFO="artifact_temp/deployment-info.json"
          echo "Deployment info:"
          jq '.' "$INFO"

          MISSING=""
          for FIELD in job_id environment target_org slack_channel commit_sha branch actor github_run_url started_at; do
            VALUE=$(jq -r --arg f "$FIELD" '.[$f] // empty' "$INFO")
            [ -z "$VALUE" ] && MISSING="$MISSING $FIELD"
          done

          if [ -n "$MISSING" ]; then
            echo "::error::Missing required fields:$MISSING"
            echo "valid=false" >> "$GITHUB_OUTPUT"
            echo "DELETE_ARTIFACT=true" >> "$GITHUB_ENV"
            exit 0
          fi

          echo "valid=true" >> "$GITHUB_OUTPUT"

          {
            echo "DEPLOY_JOB_ID=$(jq -r '.job_id' "$INFO")"
            echo "DEPLOY_ENVIRONMENT=$(jq -r '.environment' "$INFO")"
            echo "DEPLOY_ENV_UPPER=$(jq -r '.environment | ascii_upcase' "$INFO")"
            echo "DEPLOY_TARGET_ORG=$(jq -r '.target_org' "$INFO")"
            echo "DEPLOY_SLACK_CHANNEL=$(jq -r '.slack_channel' "$INFO")"
            echo "DEPLOY_COMMIT_SHA=$(jq -r '.commit_sha' "$INFO")"
            echo "DEPLOY_BRANCH=$(jq -r '.branch' "$INFO")"
            echo "DEPLOY_ACTOR=$(jq -r '.actor' "$INFO")"
            echo "DEPLOY_GITHUB_RUN_URL=$(jq -r '.github_run_url' "$INFO")"
            echo "DEPLOY_STARTED_AT=$(jq -r '.started_at' "$INFO")"
            echo "DEPLOY_SOURCE_REPO=$(jq -r '.github_repository // empty' "$INFO")"
            echo "DEPLOY_SOURCE_SERVER=$(jq -r '.github_server_url // empty' "$INFO")"
            echo "DEPLOY_PR_NUMBER=$(jq -r '.pr_number // empty' "$INFO")"
          } >> "$GITHUB_ENV"

      - name: Resolve URLs
        if: steps.parse.outputs.valid == 'true'
        run: |
          if [ -n "$DEPLOY_SOURCE_REPO" ] && [ -n "$DEPLOY_SOURCE_SERVER" ]; then
            echo "COMMIT_URL=${DEPLOY_SOURCE_SERVER}/${DEPLOY_SOURCE_REPO}/commit/${DEPLOY_COMMIT_SHA}" >> "$GITHUB_ENV"
            echo "PR_URL=${DEPLOY_SOURCE_SERVER}/${DEPLOY_SOURCE_REPO}/pull/${DEPLOY_PR_NUMBER}" >> "$GITHUB_ENV"
          else
            echo "COMMIT_URL=${{ github.server_url }}/${{ github.repository }}/commit/${DEPLOY_COMMIT_SHA}" >> "$GITHUB_ENV"
            echo "PR_URL=${{ github.server_url }}/${{ github.repository }}/pull/${DEPLOY_PR_NUMBER}" >> "$GITHUB_ENV"
          fi

          SF_BASE_URL_VAR="SF_BASE_URL_${DEPLOY_ENV_UPPER}"
          SF_BASE_URL="${!SF_BASE_URL_VAR}"
          if [ -n "$SF_BASE_URL" ]; then
            echo "SF_DEPLOY_STATUS_URL=${SF_BASE_URL}/changemgmt/monitorDeploymentsDetails.apexp?asyncId=${DEPLOY_JOB_ID}" >> "$GITHUB_ENV"
          fi

      - name: Check staleness
        id: staleness
        if: steps.parse.outputs.valid == 'true'
        run: |
          if ! STARTED_EPOCH=$(date -d "$DEPLOY_STARTED_AT" +%s 2>/dev/null); then
            echo "::error::Failed to parse timestamp: $DEPLOY_STARTED_AT"
            echo "is_stale=true" >> "$GITHUB_OUTPUT"
            echo "age_hours=unknown" >> "$GITHUB_OUTPUT"
            echo "DELETE_ARTIFACT=true" >> "$GITHUB_ENV"
            exit 0
          fi

          NOW_EPOCH=$(date +%s)
          AGE_HOURS=$(( (NOW_EPOCH - STARTED_EPOCH) / 3600 ))
          echo "age_hours=$AGE_HOURS" >> "$GITHUB_OUTPUT"

          if [ "$AGE_HOURS" -gt 6 ]; then
            echo "::warning::Deployment is ${AGE_HOURS}h old — marking as stale"
            echo "is_stale=true" >> "$GITHUB_OUTPUT"
            echo "DELETE_ARTIFACT=true" >> "$GITHUB_ENV"
          else
            echo "Deployment age: ${AGE_HOURS}h (active)"
            echo "is_stale=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Notify stale deployment
        if: steps.staleness.outputs.is_stale == 'true'
        run: |
          SF_BUTTON="null"
          if [ -n "${SF_DEPLOY_STATUS_URL:-}" ]; then
            SF_BUTTON=$(jq -n --arg url "$SF_DEPLOY_STATUS_URL" \
              '{type: "button", text: {type: "plain_text", text: "🔍 Check in Salesforce"}, url: $url}')
          fi

          PAYLOAD=$(jq -n \
            --arg channel "$DEPLOY_SLACK_CHANNEL" \
            --arg fallback "Stale deployment detected in $DEPLOY_ENVIRONMENT" \
            --arg env "$DEPLOY_ENVIRONMENT" \
            --arg age "${{ steps.staleness.outputs.age_hours }}" \
            --arg job_id "$DEPLOY_JOB_ID" \
            --arg run_url "$DEPLOY_GITHUB_RUN_URL" \
            --argjson sf_button "$SF_BUTTON" \
            '{
              channel: $channel,
              text: $fallback,
              blocks: [
                {
                  type: "section",
                  text: {
                    type: "mrkdwn",
                    text: ("⚠️ *Stale Deployment Detected*\n\nA deployment to *" + $env + "* started " + $age + " hours ago and never completed.\nJob ID: `" + $job_id + "`\n\nThis artifact will be removed. Please check Salesforce or re-run the workflow if needed.")
                  }
                },
                {
                  type: "actions",
                  elements: (
                    [{type: "button", text: {type: "plain_text", text: "📋 View Workflow"}, url: $run_url}]
                    + (if $sf_button != null then [$sf_button] else [] end)
                  )
                }
              ]
            }')

          curl -s -X POST https://slack.com/api/chat.postMessage \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $SLACK_TOKEN" \
            -d "$PAYLOAD"

      - name: Authenticate to Salesforce
        if: steps.parse.outputs.valid == 'true' && steps.staleness.outputs.is_stale != 'true'
        run: |
          SF_LOGIN_URL_VAR="SF_LOGIN_URL_${DEPLOY_ENV_UPPER}"
          SF_USERNAME_VAR="SF_USERNAME_${DEPLOY_ENV_UPPER}"
          SF_CLIENT_ID_VAR="SF_CLIENT_ID_${DEPLOY_ENV_UPPER}"
          SF_CLIENT_KEY_VAR="SF_CLIENT_KEY_${DEPLOY_ENV_UPPER}"

          SF_LOGIN_URL="${!SF_LOGIN_URL_VAR}"
          SF_USERNAME="${!SF_USERNAME_VAR}"
          SF_CLIENT_ID="${!SF_CLIENT_ID_VAR}"
          SF_CLIENT_KEY="${!SF_CLIENT_KEY_VAR}"

          if [ -z "$SF_LOGIN_URL" ] || [ -z "$SF_USERNAME" ] || [ -z "$SF_CLIENT_ID" ] || [ -z "$SF_CLIENT_KEY" ]; then
            echo "::error::Missing SF credentials for environment: $DEPLOY_ENVIRONMENT"
            exit 1
          fi

          printf '%s' "$SF_CLIENT_KEY" | base64 -d > jwt-key.key
          sf org login jwt \
            --instance-url "$SF_LOGIN_URL" \
            --username "$SF_USERNAME" \
            --client-id "$SF_CLIENT_ID" \
            --jwt-key-file jwt-key.key \
            --alias "$DEPLOY_TARGET_ORG"
          rm -f jwt-key.key

      - name: Check deployment status
        id: check_status
        if: steps.parse.outputs.valid == 'true' && steps.staleness.outputs.is_stale != 'true'
        run: |
          echo "Checking deployment status for Job ID: $DEPLOY_JOB_ID"
          DEPLOY_REPORT=$(sf project deploy report \
            --job-id "$DEPLOY_JOB_ID" \
            --target-org "$DEPLOY_TARGET_ORG" \
            --json 2>&1) || true

          STATUS=$(echo "$DEPLOY_REPORT" | jq -r '.result.status // .status // "Unknown"' 2>/dev/null) || STATUS="Unknown"
          echo "Deployment status: $STATUS"
          echo "status=$STATUS" >> "$GITHUB_OUTPUT"

          if [[ "$STATUS" =~ ^(Succeeded|Failed|Canceled)$ ]]; then
            echo "DELETE_ARTIFACT=true" >> "$GITHUB_ENV"
          fi

          echo "$DEPLOY_REPORT" > deploy_report.json

      - name: Notify success
        if: steps.check_status.outputs.status == 'Succeeded'
        run: |
          PR_FIELD="null"
          if [ -n "$DEPLOY_PR_NUMBER" ]; then
            PR_FIELD=$(jq -n --arg url "$PR_URL" --arg num "$DEPLOY_PR_NUMBER" \
              '{type: "mrkdwn", text: ("*PR:*\n<" + $url + "|#" + $num + ">")}')
          fi

          SF_BUTTON="null"
          if [ -n "${SF_DEPLOY_STATUS_URL:-}" ]; then
            SF_BUTTON=$(jq -n --arg url "$SF_DEPLOY_STATUS_URL" \
              '{type: "button", text: {type: "plain_text", text: "🔍 SF Deploy Status"}, url: $url}')
          fi

          PAYLOAD=$(jq -n \
            --arg channel "$DEPLOY_SLACK_CHANNEL" \
            --arg fallback "Salesforce deployment succeeded in $DEPLOY_ENVIRONMENT" \
            --arg env "$DEPLOY_ENVIRONMENT" \
            --arg branch "$DEPLOY_BRANCH" \
            --arg commit_url "$COMMIT_URL" \
            --arg short_sha "${DEPLOY_COMMIT_SHA:0:7}" \
            --arg actor "$DEPLOY_ACTOR" \
            --arg job_id "$DEPLOY_JOB_ID" \
            --arg run_url "$DEPLOY_GITHUB_RUN_URL" \
            --argjson pr_field "$PR_FIELD" \
            --argjson sf_button "$SF_BUTTON" \
            '{
              channel: $channel,
              text: $fallback,
              blocks: [
                {
                  type: "section",
                  text: {type: "mrkdwn", text: "✅ *Salesforce Deployment Succeeded*"}
                },
                {
                  type: "section",
                  fields: (
                    [
                      {type: "mrkdwn", text: ("*Environment:*\n" + $env)},
                      {type: "mrkdwn", text: ("*Branch:*\n" + $branch)}
                    ]
                    + (if $pr_field != null then [$pr_field] else [] end)
                    + [
                      {type: "mrkdwn", text: ("*Commit:*\n<" + $commit_url + "|`" + $short_sha + "`>")},
                      {type: "mrkdwn", text: ("*Deployed by:*\n" + $actor)},
                      {type: "mrkdwn", text: ("*Job ID:*\n`" + $job_id + "`")}
                    ]
                  )
                },
                {
                  type: "actions",
                  elements: (
                    [
                      {type: "button", text: {type: "plain_text", text: "📋 View Workflow"}, url: $run_url},
                      {type: "button", text: {type: "plain_text", text: "🔗 View Commit"}, url: $commit_url}
                    ]
                    + (if $sf_button != null then [$sf_button] else [] end)
                  )
                }
              ]
            }')

          curl -s -X POST https://slack.com/api/chat.postMessage \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $SLACK_TOKEN" \
            -d "$PAYLOAD"

      - name: Flush cache
        if: steps.check_status.outputs.status == 'Succeeded'
        run: |
          case "$DEPLOY_ENVIRONMENT" in
            acc)
              echo "Flushing cache for acc environment..."
              curl -s -X POST https://homeapp.web.acc.guidion.io/api/v1/cache-flush \
                -H "Content-Type: application/json" \
                -H "x-api-key: $CACHE_FLUSH_API_KEY" \
                -d "{\"userId\": \"$USER_ID_FOR_CACHE_FLUSH\"}" || echo "::warning::Cache flush failed (non-critical)"
              ;;
            prod)
              echo "Flushing cache for prod environment..."
              curl -s -X POST https://homeapp.guidion.io/api/v1/cache-flush \
                -H "Content-Type: application/json" \
                -H "x-api-key: $CACHE_FLUSH_API_KEY" \
                -d "{\"userId\": \"$USER_ID_FOR_CACHE_FLUSH\"}" || echo "::warning::Cache flush failed (non-critical)"
              ;;
            *)
              echo "No cache flush configured for $DEPLOY_ENVIRONMENT"
              ;;
          esac

      - name: Notify failure
        if: steps.check_status.outputs.status == 'Failed'
        run: |
          COMPONENT_FAILURES=$(jq -r '
            [(.result.details.componentFailures // [])
             | if type == "array" then .[] else . end
             | "• \(.componentType): \(.fullName) — \(.problem // "unknown error")"]
            | .[0:10] | join("\n")' deploy_report.json 2>/dev/null || echo "")

          TEST_FAILURES=$(jq -r '
            [(.result.details.runTestResult.failures // [])
             | if type == "array" then .[] else . end
             | "• \(.name).\(.methodName): \(.message // "test failed")"]
            | .[0:5] | join("\n")' deploy_report.json 2>/dev/null || echo "")

          GENERAL_ERROR=$(jq -r '.result.errorMessage // .message // empty' deploy_report.json 2>/dev/null || echo "")

          NL=$'\n'
          ERROR_MESSAGE=""
          [ -n "$COMPONENT_FAILURES" ] && ERROR_MESSAGE="Component Failures:${NL}${COMPONENT_FAILURES}"
          if [ -n "$TEST_FAILURES" ]; then
            [ -n "$ERROR_MESSAGE" ] && ERROR_MESSAGE="${ERROR_MESSAGE}${NL}${NL}"
            ERROR_MESSAGE="${ERROR_MESSAGE}Test Failures:${NL}${TEST_FAILURES}"
          fi
          if [ -n "$GENERAL_ERROR" ] && [ "$GENERAL_ERROR" != "null" ]; then
            [ -n "$ERROR_MESSAGE" ] && ERROR_MESSAGE="${ERROR_MESSAGE}${NL}${NL}"
            ERROR_MESSAGE="${ERROR_MESSAGE}${GENERAL_ERROR}"
          fi
          [ -z "$ERROR_MESSAGE" ] && ERROR_MESSAGE="No specific error details available. Check Salesforce deployment status for more info."

          ERROR_MESSAGE=$(printf '%.1500s' "$ERROR_MESSAGE")

          PR_FIELD="null"
          if [ -n "$DEPLOY_PR_NUMBER" ]; then
            PR_FIELD=$(jq -n --arg url "$PR_URL" --arg num "$DEPLOY_PR_NUMBER" \
              '{type: "mrkdwn", text: ("*PR:*\n<" + $url + "|#" + $num + ">")}')
          fi

          SF_BUTTON="null"
          if [ -n "${SF_DEPLOY_STATUS_URL:-}" ]; then
            SF_BUTTON=$(jq -n --arg url "$SF_DEPLOY_STATUS_URL" \
              '{type: "button", text: {type: "plain_text", text: "🔍 SF Deploy Status"}, url: $url}')
          fi

          PAYLOAD=$(jq -n \
            --arg channel "$DEPLOY_SLACK_CHANNEL" \
            --arg fallback "Salesforce deployment failed in $DEPLOY_ENVIRONMENT" \
            --arg env "$DEPLOY_ENVIRONMENT" \
            --arg branch "$DEPLOY_BRANCH" \
            --arg commit_url "$COMMIT_URL" \
            --arg short_sha "${DEPLOY_COMMIT_SHA:0:7}" \
            --arg actor "$DEPLOY_ACTOR" \
            --arg job_id "$DEPLOY_JOB_ID" \
            --arg run_url "$DEPLOY_GITHUB_RUN_URL" \
            --arg error_msg "$ERROR_MESSAGE" \
            --argjson pr_field "$PR_FIELD" \
            --argjson sf_button "$SF_BUTTON" \
            '{
              channel: $channel,
              text: $fallback,
              blocks: [
                {
                  type: "section",
                  text: {type: "mrkdwn", text: "❌ *Salesforce Deployment Failed*"}
                },
                {
                  type: "section",
                  fields: (
                    [
                      {type: "mrkdwn", text: ("*Environment:*\n" + $env)},
                      {type: "mrkdwn", text: ("*Branch:*\n" + $branch)}
                    ]
                    + (if $pr_field != null then [$pr_field] else [] end)
                    + [
                      {type: "mrkdwn", text: ("*Commit:*\n<" + $commit_url + "|`" + $short_sha + "`>")},
                      {type: "mrkdwn", text: ("*Deployed by:*\n" + $actor)},
                      {type: "mrkdwn", text: ("*Job ID:*\n`" + $job_id + "`")}
                    ]
                  )
                },
                {
                  type: "section",
                  text: {type: "mrkdwn", text: ("*Error Details:*\n```" + $error_msg + "```")}
                },
                {type: "divider"},
                {
                  type: "section",
                  text: {type: "mrkdwn", text: "*🔄 To retry:* Click \u0027View Workflow\u0027 below, then click *Re-run jobs* → *Re-run failed jobs*"}
                },
                {
                  type: "actions",
                  elements: (
                    [
                      {type: "button", text: {type: "plain_text", text: "📋 View Workflow (Re-run here)"}, url: $run_url, style: "primary"},
                      {type: "button", text: {type: "plain_text", text: "🔗 View Commit"}, url: $commit_url}
                    ]
                    + (if $sf_button != null then [$sf_button] else [] end)
                  )
                }
              ]
            }')

          curl -s -X POST https://slack.com/api/chat.postMessage \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $SLACK_TOKEN" \
            -d "$PAYLOAD"

      - name: Delete artifact
        if: always() && env.DELETE_ARTIFACT == 'true'
        run: |
          echo "Deleting artifact ${{ matrix.artifact_name }}..."
          gh api --method DELETE "repos/${{ github.repository }}/actions/artifacts/${{ matrix.artifact_id }}" \
            || echo "::warning::Failed to delete artifact ${{ matrix.artifact_name }}"

      - name: Cleanup
        if: always()
        run: rm -rf artifact_temp artifact.zip deploy_report.json
